# OrangeHRM Demo Configuration
PROJECT_NAME=OrangeHRM
BASE_URL=https://opensource-demo.orangehrmlive.com
BROWSER=chromium
HEADLESS=false

# Credentials
ADMIN_USERNAME=Admin
ADMIN_PASSWORD=admin123

# Test Configuration
DEFAULT_TIMEOUT=30000
NAVIGATION_TIMEOUT=30000
ACTION_TIMEOUT=10000

# Feature files for this project
FEATURES=test/orangehrm/features/*.feature
FEATURE_PATH=test/orangehrm/features

# Step definitions (must match actual directory structure)
STEP_DEFINITIONS_PATH=test/orangehrm/steps;test/orangehrm/step-definitions

# Disable retries to avoid infinite loops during debugging
RETRY_COUNT=0

# ====================================================================================
# OrangeHRM Test Configuration (TEMPORARY - For Framework Validation Only)
# ====================================================================================
# This configuration is for testing the zero-code feature in the framework itself.
# Will be REMOVED after successful validation before ADO commit.
# ====================================================================================

# Application URL
BASE_URL=https://opensource-demo.orangehrmlive.com/web/index.php/auth/login

# Browser Configuration
BROWSER=chromium
HEADLESS=false
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080

# Timeouts
DEFAULT_TIMEOUT=30000
NAVIGATION_TIMEOUT=60000

# Screenshot Configuration
SCREENSHOT_CAPTURE_MODE=on-failure
SCREENSHOT_ON_PASS=false
SCREENSHOT_ON_FAIL=true

# Video Configuration
VIDEO_RECORDING_ENABLED=false

# Parallel Execution
PARALLEL_WORKERS=1
PARALLEL_ENABLED=false

# Reporting
REPORT_ENABLED=true
REPORT_FORMAT=html

# Zero-Code Feature (CRITICAL - Must be enabled)
INTELLIGENT_STEP_EXECUTION_ENABLED=true

# AI Platform (Enable for AI healing tests)
AI_ENABLED=true
AI_INTELLIGENT_HEALING_ENABLED=true
AI_UI_ONLY=true
AI_MAX_HEALING_ATTEMPTS=3
AI_CONFIDENCE_THRESHOLD=0.75

# Credentials (OrangeHRM Demo)
USERNAME=Admin
PASSWORD=admin123

# Test Data
TEST_USER_FIRSTNAME=Test
TEST_USER_LASTNAME=User
TEST_USER_EMAIL=testuser@example.com

# ====================================================================================
# VALIDATION NOTES:
# ====================================================================================
# This configuration file validates:
# 1. Zero-code feature enabled (INTELLIGENT_STEP_EXECUTION_ENABLED=true)
# 2. AI platform enabled (AI_ENABLED=true)
# 3. AI healing enabled (AI_INTELLIGENT_HEALING_ENABLED=true)
# 4. UI-only mode (AI_UI_ONLY=true)
#
# After successful testing, this file will be removed before ADO commit.
# ====================================================================================
